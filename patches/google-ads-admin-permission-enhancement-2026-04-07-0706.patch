diff --git a/src/app/(app)/settings/page.tsx b/src/app/(app)/settings/page.tsx
index 008b81f..6515977 100644
--- a/src/app/(app)/settings/page.tsx
+++ b/src/app/(app)/settings/page.tsx
@@ -480,8 +480,34 @@ export default function SettingsPage() {
     router.push(`/login?redirect=${redirectUrl}`)
   }
 
+  // 获取当前用户角色
+  const fetchCurrentUser = async () => {
+    try {
+      const response = await fetch('/api/auth/me', {
+        credentials: 'include',
+      })
+      
+      if (response.ok) {
+        const data = await response.json()
+        const role = data.user?.role || 'user'
+        setCurrentUserRole(role)
+        setIsAdmin(role === 'admin')
+      } else {
+        setCurrentUserRole('user')
+        setIsAdmin(false)
+      }
+    } catch (err) {
+      console.error('Failed to fetch current user:', err)
+      setCurrentUserRole('user')
+      setIsAdmin(false)
+    } finally {
+      setLoadingUser(false)
+    }
+  }
+
   useEffect(() => {
     fetchSettings()
+    if (typeof window !== 'undefined') fetchCurrentUser()
   }, [])
 
   // 检查 OAuth 回调结果
diff --git a/src/app/api/google-ads/credentials/route.ts b/src/app/api/google-ads/credentials/route.ts
index 4dfae04..ae993e9 100644
--- a/src/app/api/google-ads/credentials/route.ts
+++ b/src/app/api/google-ads/credentials/route.ts
@@ -25,6 +25,14 @@ export async function POST(request: NextRequest) {
       )
     }
 
+    // 🔒 管理员权限检查
+    if (authResult.user.role !== 'admin') {
+      return NextResponse.json(
+        { error: '需要管理员权限' },
+        { status: 403 }
+      )
+    }
+
     // 解析请求参数
     const body = await request.json()
     const {
@@ -209,6 +217,14 @@ export async function DELETE(request: NextRequest) {
       )
     }
 
+    // 🔒 管理员权限检查
+    if (authResult.user.role !== 'admin') {
+      return NextResponse.json(
+        { error: '需要管理员权限' },
+        { status: 403 }
+      )
+    }
+
     const userId = authResult.user.userId
 
     // 1) 停用/清空 OAuth 凭证（google_ads_credentials）
@@ -265,6 +281,14 @@ export async function PATCH(request: NextRequest) {
       )
     }
 
+    // 🔒 管理员权限检查
+    if (authResult.user.role !== 'admin') {
+      return NextResponse.json(
+        { error: '需要管理员权限' },
+        { status: 403 }
+      )
+    }
+
     const userId = authResult.user.userId
     const body = await request.json()
     const { apiAccessLevel } = body
diff --git a/src/app/api/google-ads/oauth/start/route.ts b/src/app/api/google-ads/oauth/start/route.ts
index afdc117..848a5cd 100644
--- a/src/app/api/google-ads/oauth/start/route.ts
+++ b/src/app/api/google-ads/oauth/start/route.ts
@@ -25,6 +25,14 @@ export async function GET(request: NextRequest) {
       )
     }
 
+    // 🔒 管理员权限检查
+    if (authResult.user.role !== 'admin') {
+      return NextResponse.json(
+        { error: '需要管理员权限' },
+        { status: 403 }
+      )
+    }
+
     const userId = authResult.user.userId
 
     console.log(`🔐 [OAuth Start] 用户ID: ${userId}`)
diff --git a/src/app/api/google-ads/service-account/route.ts b/src/app/api/google-ads/service-account/route.ts
index 7e94058..7f45c88 100644
--- a/src/app/api/google-ads/service-account/route.ts
+++ b/src/app/api/google-ads/service-account/route.ts
@@ -16,6 +16,11 @@ export async function POST(req: NextRequest) {
     return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })
   }
 
+  // 🔒 管理员权限检查
+  if (user.role !== 'admin') {
+    return NextResponse.json({ error: '需要管理员权限' }, { status: 403 })
+  }
+
   try {
     const { name, mccCustomerId, developerToken, serviceAccountJson } = await req.json()
 
@@ -69,6 +74,11 @@ export async function DELETE(req: NextRequest) {
     return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })
   }
 
+  // 🔒 管理员权限检查
+  if (user.role !== 'admin') {
+    return NextResponse.json({ error: '需要管理员权限' }, { status: 403 })
+  }
+
   try {
     const { searchParams } = new URL(req.url)
     const id = searchParams.get('id')
diff --git a/src/app/api/settings/route.ts b/src/app/api/settings/route.ts
index c1912cc..d776b8c 100644
--- a/src/app/api/settings/route.ts
+++ b/src/app/api/settings/route.ts
@@ -206,6 +206,21 @@ export async function PUT(request: NextRequest) {
 
     const { updates } = validationResult.data
 
+    // 🔒 检查是否有 Google Ads 配置的更新
+    const hasGoogleAdsUpdate = updates.some(u => u.category === 'google_ads')
+    if (hasGoogleAdsUpdate) {
+      // 获取当前用户信息
+      const { findUserById } = await import('@/lib/auth')
+      const user = userIdNum ? await findUserById(userIdNum) : null
+      
+      if (!user || user.role !== 'admin') {
+        return NextResponse.json(
+          { error: 'Google Ads API 配置需要管理员权限' },
+          { status: 403 }
+        )
+      }
+    }
+
     // 联盟同步配置必须是用户级，不允许无用户上下文写入
     const hasAffiliateSyncUpdate = updates.some((update) => update.category === 'affiliate_sync')
     if (hasAffiliateSyncUpdate && !userIdNum) {
