import { RSA_QUALITY_MINIMUM_SCORE } from './rsa-quality-gate'

/* * 标准模式下的最大自动重试次数（与质量循环默认一致） */
export const AD_CREATIVE_MAX_AUTO_RETRIES = 2

/* * 创意质量达标最低分（与 RSA 质量门禁一致） */
export const AD_CREATIVE_REQUIRED_MIN_SCORE = RSA_QUALITY_MINIMUM_SCORE
